name: Build Release DMG

permissions:
  contents: write

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build:
    name: Build DMG
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install cargo-bundle
        run: cargo install cargo-bundle

      - name: Build DMG
        run: cargo bundle --release

      - name: Verify DMG exists
        run: |
          echo "=== Checking build output ==="
          ls -lah target/release/bundle/dmg/
          file target/release/bundle/dmg/Ticker.dmg

      - name: Check DMG integrity
        run: |
          echo "=== DMG integrity check ==="
          hdiutil verify target/release/bundle/dmg/Ticker.dmg || echo "⚠️ DMG warning (but continuing)"

      - name: Rename DMG with version
        run: |
          VERSION=${{ github.ref_name }}
          mv target/release/bundle/dmg/Ticker.dmg target/release/bundle/dmg/Ticker-${VERSION}.dmg
          ls -lah target/release/bundle/dmg/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: target/release/bundle/dmg/Ticker-*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release summary
        if: success()
        run: echo "✅ Release created successfully with version tag!"
